<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
   "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
   "http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>
	<constant name="struts.devMode" value="true" />

	<package name="default" extends="struts-default, json-default">

		<interceptors>
			<interceptor-stack name="myStack">
				<interceptor-ref name="defaultStack" />
				<interceptor-ref name="json" />
			</interceptor-stack>
		</interceptors>

		<action name="login" class="com.workforce.actions.Login">
			<interceptor-ref name="myStack" />
			<result name="success" type="json">
				<param name="excludeNullProperties">true</param>
				<param name="includeProperties">
					user\.accessToken,
					user\.accountTypeModel\.type
				</param>
			</result>
			
			<result name="input" type="json">
				<param name="statusCode">500</param>
				<param name="errorCode">500</param>
			</result>
		</action>
		
		<action name="getDashboardUserDetails" class="com.workforce.actions.GetDashboardUserDetails">
			<interceptor-ref name="myStack" />
			<result name="success" type="json">
				<param name="excludeNullProperties">true</param>
				<param name="includeProperties">
					user\.accessToken,
					user\.accountTypeModel\.type,
					user\.participantModel\.firstname,
					user\.participantModel\.imageURL,
					user\.participantModel\.lastname
				</param>
			</result>
			
			<result name="input" type="json">
				<param name="statusCode">500</param>
				<param name="errorCode">500</param>
			</result>
		</action>
	</package>
	
	<include file="struts_participant"></include>
</struts>